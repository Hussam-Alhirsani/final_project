<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shortstories">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_short_stories"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_short_stories"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyStory">
        <activity
            android:name="com.example.shortstories.BaseActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>